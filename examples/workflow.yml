name: CLA Check

on:
  pull_request_target:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]

permissions:
  contents: write
  pull-requests: write
  issues: write
  checks: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CLA Bot
        uses: overtrue/cla-bot@v0.0.4
        with:
          github-token: ${{ github.token }}
          # PAT or GitHub App token with write access to the registry repository.
          registry-token: ${{ secrets.CLA_BOT_REGISTRY_TOKEN }}
